async (page) => {  console.log("点击 4:3 比例标签...");  await page.locator(':text("首页推荐封面")').first().click().catch(() => {});  await page.waitForTimeout(1000);  const triggers = page.locator(':text("上传封面"), :text("拖拽图片或点击上传")');  if (await triggers.count() > 0) {    console.log("找到触发器，准备上传...");    await triggers.first().click({ timeout: 3000 });    return "chooser1_triggered";  }  return "no_trigger";}